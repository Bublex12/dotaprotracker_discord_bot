# 🐳 Запуск бота через Docker

## Быстрый старт

### 1. Создайте файл `.env` в корне проекта:

```env
DISCORD_BOT_TOKEN=ваш_токен_бота_здесь
```

### 2. Запустите через Docker Compose:

```bash
docker-compose up -d
```

### 3. Проверьте логи:

```bash
docker-compose logs -f
```

## Команды Docker Compose

```bash
# Запустить бота
docker-compose up -d

# Остановить бота
docker-compose down

# Перезапустить бота
docker-compose restart

# Посмотреть логи
docker-compose logs -f

# Пересобрать образ
docker-compose build --no-cache

# Остановить и удалить контейнер
docker-compose down
```

## Только Docker (без Compose)

### 1. Соберите образ:

```bash
docker build -t dota-discord-bot .
```

### 2. Запустите контейнер:

```bash
docker run -d \
  --name dota-discord-bot \
  --restart unless-stopped \
  -e DISCORD_BOT_TOKEN=ваш_токен_бота \
  -v $(pwd)/screenshots:/app/screenshots \
  dota-discord-bot
```

## Переменные окружения

Создайте файл `.env` или передайте переменные напрямую:

```env
DISCORD_BOT_TOKEN=ваш_токен_бота
```

## Структура

```
dota-discord-bot/
├── Dockerfile              # Образ Docker
├── docker-compose.yml      # Конфигурация Compose
├── .dockerignore           # Исключения для Docker
├── .env                    # Переменные окружения (создать)
├── main.py                 # Основной файл бота
├── screenshot_hero.py      # Скрипт скриншотов
└── requirements.txt        # Python зависимости
```

## Размещение на хостинге

### VPS (DigitalOcean, Hetzner, и т.д.)

1. Установите Docker и Docker Compose на сервер
2. Скопируйте файлы проекта на сервер
3. Создайте `.env` файл с токеном
4. Запустите: `docker-compose up -d`

### Railway, Render, Fly.io

Эти платформы поддерживают Docker:
1. Подключите GitHub репозиторий
2. Укажите Dockerfile
3. Добавьте переменную окружения `DISCORD_BOT_TOKEN`
4. Платформа автоматически соберет и запустит контейнер

## Обновление бота

```bash
# Остановите контейнер
docker-compose down

# Обновите код (git pull или загрузите новые файлы)

# Пересоберите образ
docker-compose build --no-cache

# Запустите снова
docker-compose up -d
```

## Решение проблем

### Бот не запускается:
- Проверьте логи: `docker-compose logs`
- Убедитесь, что токен в `.env` правильный
- Проверьте, что порты не заняты

### Скриншоты не создаются:
- Проверьте права на директорию `screenshots`
- Убедитесь, что браузер установлен: `docker-compose exec discord-bot playwright install chromium`

### Контейнер падает:
- Проверьте логи: `docker-compose logs -f`
- Убедитесь, что все зависимости установлены
- Проверьте доступность интернета из контейнера

## Мониторинг

```bash
# Статус контейнера
docker-compose ps

# Использование ресурсов
docker stats dota-discord-bot

# Логи в реальном времени
docker-compose logs -f discord-bot
```

