# Dota 2 Hero Screenshot Bot

Discord бот для создания скриншотов билдов героев с сайта [dota2protracker.com](https://dota2protracker.com/hero).

## Возможности

- 🤖 Discord бот с командами для получения скриншотов
- 📸 Автоматическое создание скриншотов вкладки Builds
- 🚀 Быстрая работа с оптимизированными таймаутами
- 🎯 Поддержка всех героев Dota 2

## Установка

1. **Установите зависимости через uv:**
```bash
uv sync
```

   Или если используете pip:
```bash
pip install -r requirements.txt
```

2. **Установите браузеры для Playwright:**
```bash
playwright install chromium
```

   Или через uv:
```bash
uv run playwright install chromium
```

3. **Настройте Discord бота:**
   - Создайте бота на [Discord Developer Portal](https://discord.com/developers/applications)
   - Скопируйте токен бота
   - Создайте файл `.env` в корне проекта:
   ```
   DISCORD_BOT_TOKEN=ваш_токен_бота_здесь
   ```

4. **Пригласите бота на сервер:**
   - В разделе OAuth2 → URL Generator выберите:
     - Scopes: `bot`, `applications.commands`
     - Bot Permissions: `Send Messages`, `Attach Files`, `Read Message History`
   - Используйте сгенерированную ссылку для приглашения

## Использование

### Запуск Discord бота:

С использованием uv:
```bash
uv run python main.py
```

Или напрямую:
```bash
python main.py
```

### Команды бота:

- `!hero <название>` - Получить скриншот билда героя
  - Примеры: `!hero mars`, `!hero pudge`, `!hero invoker`
  - Алиасы: `!h <название>`, `!герой <название>`

- `!help_hero` - Показать справку по использованию

### Использование скрипта напрямую:

Если нужно использовать скрипт без Discord бота:

```bash
# Измените переменную HERO_NAME в screenshot_hero.py
python screenshot_hero.py
```

Или отредактируйте файл `screenshot_hero.py`:

```python
HERO_NAME = "mars"  # Название героя
OUTPUT_DIR = "screenshots"  # Папка для сохранения
WAIT_TIME = 200  # Время ожидания в мс
```

## Примеры использования

### В Discord:

```
!hero mars
!hero pudge
!h invoker
!герой antimage
```

### Прямое использование скрипта:

Скриншоты сохраняются в папку `screenshots/` с именем `{hero_name}.png`.

## Структура проекта

```
dota-discord-bot/
├── main.py              # Discord бот
├── screenshot_hero.py   # Скрипт создания скриншотов
├── requirements.txt     # Зависимости
├── .env                 # Токен бота (создать самостоятельно)
├── screenshots/         # Папка со скриншотами
└── README.md           # Документация
```

## Решение проблем

### Бот не отвечает:
- Проверьте, что токен в `.env` правильный
- Убедитесь, что бот приглашен на сервер
- Проверьте права бота (Send Messages, Attach Files)

### Скриншоты не создаются:
- Убедитесь, что установлен браузер: `playwright install chromium`
- Проверьте подключение к интернету
- Посмотрите логи в консоли для подробностей ошибок

## Лицензия

MIT
